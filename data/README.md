Put the ADP historical CSV here as `adp_history.csv`.

Expected columns include:
- date
- NER_SA

If the raw ADP file contains multiple categories, filter it to the total private employment series in `data.py` after inspecting the category/agg_RIS values.
